/**
 * StatusBar Component
 *
 * System status information is now displayed in the sidebar.
 * This component is kept as an empty placeholder to maintain layout structure.
 */

export default function StatusBar() {
  return (
    <div className="status-bar">
      {/* System status moved to sidebar - keeping empty container for layout */}
    </div>
  );
}
